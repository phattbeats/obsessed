services:
  obsessed:
    build: .
    image: phattbeatts/obsessed:latest
    ports:
      - "8000:8000"
    volumes:
      - obsessed-data:/app/data
    restart: unless-stopped

volumes:
  obsessed-data: