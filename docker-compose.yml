services:
  obsessed:
    build: .
    image: therealphatt/obsessed:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - obsessed-data:/app/data
    restart: unless-stopped

volumes:
  obsessed-data: