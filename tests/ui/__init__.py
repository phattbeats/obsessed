# UI tests for Obsessed trivia game — requires a live server.
# Use PLAYWRIGHT_BASE_URL env var to override the default (http://10.0.0.100:10198).
# In CI these run against the container; locally against the dev server.